<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="event_registration.list">
      <field name="name">event_registration list</field>
      <field name="model">event_registration.event_registration</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="event_registration.action_window">
      <field name="name">event_registration window</field>
      <field name="res_model">event_registration.event_registration</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="event_registration.action_server">
      <field name="name">event_registration server</field>
      <field name="model_id" ref="model_event_registration_event_registration"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="event_registration" id="event_registration.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="event_registration.menu_1" parent="event_registration.menu_root"/>
    <menuitem name="Menu 2" id="event_registration.menu_2" parent="event_registration.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="event_registration.menu_1_list" parent="event_registration.menu_1"
              action="event_registration.action_window"/>
    <menuitem name="Server to list" id="event_registration" parent="event_registration.menu_2"
              action="event_registration.action_server"/>
    -->
  </data>
</odoo>